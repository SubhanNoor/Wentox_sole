# utils/ — Shared Helpers

Small, dependency-free helpers used across layers. Keep this folder tiny — if something contains
business rules it belongs in a service, if it touches the DB it belongs in a repository.

Currently empty. Expected additions during the milestones:
- date-range helpers for the Weekly / Monthly / Overall list filters
- pagination / query-param parsing helpers
